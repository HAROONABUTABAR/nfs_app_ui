import 'package:flutter/material.dart';

import 'upload_item_done_content.dart';

class UploadItemDoneView extends StatelessWidget {
  const UploadItemDoneView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: UploadItemDoneContent(),
    );
  }
}
